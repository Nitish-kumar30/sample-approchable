# Vercel Blob (from Vercel project → Storage → Blob → Connect)
# Required — our routes and @vercel/blob use this (auto-set on Vercel when Blob is connected)
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_nEU8wy7d5Cfs6fvd_FS6MFWV7gg63Ufct7hWftlX4L51GKK"
# Optional — Vercel may also set this; used by @vercel/blob with OIDC auth (not read in our app code)
BLOB_STORE_ID="store_nEU8wy7d5Cfs6fvd"
# Admin (optional for now — open if unset)
# Optional admin gate (leave unset to show submissions without a secret)
# ADMIN_SECRET=
